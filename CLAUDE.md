# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 가이드입니다.

## 프로젝트 개요

마크다운(.md) 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트입니다.
사용자가 작성한 `.md` 글 파일들을 파싱하여 목록 페이지와 개별 글 페이지로 렌더링합니다.

## 기술 스택 / 제약 사항

- **프레임워크 사용 금지**: React, Vue, Next.js 등 어떤 프론트엔드 프레임워크도 사용하지 않습니다.
- **빌드 도구 없음**: Webpack, Vite, bundler 등을 도입하지 않습니다. 브라우저에서 바로 열리거나 정적 서버로 서빙 가능해야 합니다.
- 순수 **HTML, CSS, JavaScript(Vanilla JS)** 로만 구현합니다.
- 마크다운 파싱 라이브러리(marked.js 등 단일 파일 CDN/로컬 스크립트)를 쓸지, 직접 파서를 구현할지는 작업 시작 전 사용자와 확인합니다. 기본값은 의존성을 최소화하는 방향입니다.
- Node.js는 로컬 정적 서버 실행(`npx serve`, `http-server` 등) 용도로만 쓰고, 런타임 의존성으로 두지 않습니다.

## 디자인 요구사항

- **깔끔하고 읽기 좋은(readable) 타이포그래피** 중심 디자인. 본문 가독성을 최우선으로 합니다(적절한 line-height, 본문 폭 제한 등).
- **다크 모드 지원**: `prefers-color-scheme`을 기본으로 따르고, 사용자가 수동으로 전환할 수 있는 토글도 제공합니다. 선택한 테마는 `localStorage`에 저장해 새로고침 후에도 유지합니다.
- **모바일 반응형**: 작은 화면에서도 레이아웃이 깨지지 않아야 합니다. 미디어 쿼리 기반으로 구현하고, 실제 모바일 뷰포트에서 확인합니다.
- 불필요한 장식보다는 콘텐츠(글) 중심의 미니멀한 스타일을 지향합니다.

## 작업 시 유의사항

- 새 기능을 추가할 때 프레임워크나 빌드 도구를 끌어오고 싶어지는 상황이 생기면, 먼저 순수 JS로 구현 가능한지 검토하고 사용자에게 확인 후 진행합니다.
- UI/디자인 변경 시에는 브라우저에서 실제로 라이트/다크 모드와 모바일 크기를 확인한 뒤 완료로 보고합니다.
